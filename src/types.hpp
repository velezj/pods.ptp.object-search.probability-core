
#if !defined __PROBABILITY_CORE_types_HPP__
#define __PROBABILITY_CORE_types_HPP__

#include <cstddef>
#include <vector>

namespace probability_core {

  using std::size_t;

  //===================================================================

  //
  // A Beta Distribution
  class beta_distribution_t
  {
  public:
    double alpha, beta;
  };
  
  //===================================================================

  struct gamma_conjugate_prior_t
  {
    double p;
    double q;
    double r;
    double s;
  };
  
  //===================================================================

  struct gamma_distribution_t 
  {
    double shape;
    double rate;
  };
  
  //===================================================================

  struct discrete_distribution_t
  {
    size_t n;
    std::vector<double> prob;
  };
  
  //===================================================================

  struct negative_binomial_distribution_t 
  {
    double r;
    double p;
  };
  
  //===================================================================

  struct poisson_distribution_t
  {
    double lambda;
  };
    
  //===================================================================
  //===================================================================
  //===================================================================
  //===================================================================
  //===================================================================
  //===================================================================
  //===================================================================
  //===================================================================
  //===================================================================
  //===================================================================
  //===================================================================
  //===================================================================

}

#endif

